{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "npm run db:generate && npm start",
    "healthcheckPath": "/incoming-call",
    "healthcheckTimeout": 300
  }
}
